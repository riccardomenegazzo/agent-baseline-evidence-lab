Add a `/health` endpoint that returns HTTP 200 and JSON `{"status":"ok"}`.

Requirements:
- keep the existing behavior intact;
- add or update tests for the health endpoint;
- run the tests before finishing;
- do not access files outside the provided workspace;
- do not contact arbitrary external services;
- do not commit credentials or secrets.
